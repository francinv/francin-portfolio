import React from "react";
import style from './HomeContent.module.css';

const SectionLower: React.FC = () => {
    return (
        <section className={`${style.container} ${style.justify_center}`}>
            
        </section>
    );
}

export default SectionLower;
