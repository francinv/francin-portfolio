import React from "react";
import style from "./Card.module.css";

const GithubOverviewCard = () => {
    return (
        <div className={`${style.rounded_card} ${style.card_container} ${style.dark_card}`}>

        </div>
    )
}